import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Mia Malkova - Elegant Companion & Media Personality",
  description: "Step into a world of refined companionship and unforgettable experiences with Mia Malkova. Exclusive experiences, wherever you are.",
  keywords: "Mia Malkova, companion, elegant, exclusive, Palm Springs, California",
  authors: [{ name: "Mia Malkova" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
